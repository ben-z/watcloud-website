# Machines

import { MachineCard } from '@/components/machine-card'
import { machineInfo } from '@/lib/data' 
import { pluralizeWithCount } from '@/lib/utils'

There are
[{pluralizeWithCount(machineInfo.machines['slurm_compute_nodes'].length, "SLURM compute node")}](#compute-nodes)
and [{pluralizeWithCount(machineInfo.machines['slurm_login_nodes'].length, "SLURM login node")}](#login-nodes)
hosted on [{pluralizeWithCount(machineInfo.machines['bare_metals'].length, "bare-metal machine")}](#bare-metal-machines)
in the cluster.

### Compute Nodes

The following machines are used to run jobs. To use them, refer to the [SLURM](/docs/compute-cluster/slurm) guide for instructions on how to submit jobs.

{
    <div className="items-start justify-center gap-6 rounded-lg p-8 grid md:grid-cols-2 xl:grid-cols-3">
      { machineInfo.machines['slurm_compute_nodes'].map((machine, index) => <MachineCard key={index} machine={machine} />) }
    </div>
}

### Login Nodes

Use the following machines to submit jobs. To get started, follow the [SSH](/docs/compute-cluster/ssh) guide to connect, then refer to the [SLURM](/docs/compute-cluster/slurm) guide to submit your jobs.

{
    <div className="items-start justify-center gap-6 rounded-lg p-8 grid md:grid-cols-2 xl:grid-cols-3">
      { machineInfo.machines['slurm_login_nodes'].map((machine, index) => <MachineCard key={index} machine={machine} />) }
    </div>
}

### General-Use Machines (Legacy)

The following machines are legacy general-use machines. They will be decommissioned in the near future.

{
    <div className="items-start justify-center gap-6 rounded-lg p-8 grid md:grid-cols-2 xl:grid-cols-3">
      { machineInfo.machines['legacy_general_use_machines'].map((machine, index) => <MachineCard key={index} machine={machine} />) }
    </div>
}

### Bastion Hosts (Legacy)

The following machines are legacy bastion hosts. They will be decommissioned in the near future.

{
    <div className="items-start justify-center gap-6 rounded-lg p-8 grid md:grid-cols-2 xl:grid-cols-3">
      { machineInfo.machines['bastions'].map((machine, index) => <MachineCard key={index} machine={machine} />) }
    </div>
}

### Bare-Metal Machines

The following machines are reserved for running supporting services (hypervisor, distributed filesystem, NAS, etc.) for the cluster.
They are accessible only to the cluster administrators.

{
    <div className="items-start justify-center gap-6 rounded-lg p-8 grid md:grid-cols-2 xl:grid-cols-3">
      { machineInfo.machines['bare_metals'].map((machine, index) => <MachineCard key={index} machine={machine} showStatusIndicator={false} />) }
    </div>
}
